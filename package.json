{
  "name": "clash-party-custom-override",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/merge-custom-rules.mjs",
    "audit:direct": "node scripts/audit-direct-priority.mjs",
    "check": "npm run build && npm run audit:direct && node --check dist/Smart-Override.js"
  },
  "engines": {
    "node": ">=14.21"
  }
}
